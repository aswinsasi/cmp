#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/npx$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/npx$exe" tsx "$basedir/../@cmp/cli/src/cli.ts" $args
  } else {
    & "$basedir/npx$exe" tsx "$basedir/../@cmp/cli/src/cli.ts" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "npx$exe" tsx "$basedir/../@cmp/cli/src/cli.ts" $args
  } else {
    & "npx$exe" tsx "$basedir/../@cmp/cli/src/cli.ts" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
